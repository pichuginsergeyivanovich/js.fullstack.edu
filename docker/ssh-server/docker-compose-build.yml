version: '2.4'

networks:
  jshw:
    driver: bridge

services:
  gitcraft:
    build: .
    image: ssh-server-gitcraft:latest
    container_name: gitcraft
    restart: always
#    volumes:
#      - ./data:/var/lib/postgresql/data
    networks:
      - jshw
#    environment:
    ports:
      - 2222:22


